<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>Consent</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css">
    <link href="../css/login.css" rel="stylesheet">
  </head>

  <body>
    <div class="container">
      <form class="form-signin" action="/consent" method="post">
        <h3 class="form-signin-heading">Hi {{ user }} </h3>
        <h5 class="form-signin-heading"> {{ client.client_id }} requests access to your data! </h5>
        </br>
        </br>

        {{#each requested_scope}}
          <div class="form-check">
            <label class="form-check-label">
              <input class="form-check-input" type="checkbox" value={{this}} name="grant_scope">
              {{this}}
            </label>
          </div>
        {{/each}}
        <input type="hidden" name="challenge" value={{ challenge }}>

        </br>
        <div class="form-check">
          <label class="form-check-label">
            <input class="form-check-input" type="checkbox" value="remember" name="remember">
                  Dont ask me again
          </label>
        </div>

        <button class="btn btn-lg btn-primary btn-block" name="submit" value="Allow Access" type="submit">Allow Access</button>
        <button class="btn btn-lg btn-danger btn-block" name="submit" value="Deny Access" type="submit">Deny Access</button>
      </form>
    </div>
  </body>
</html>
